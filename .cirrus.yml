build_task:
  name: "Build Docker Image"
  container:
    image: docker:latest
  start_dockerd_background_script:
    dockerd
  build_script:
    docker build -t worker .

task:
  name: "Staging Deploy"
  container:
    image: docker:latest
  trigger_type: manual
